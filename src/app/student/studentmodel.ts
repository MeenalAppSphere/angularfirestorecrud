import { FormGroup, FormBuilder } from "@angular/forms";

export interface StudentModel{
  id:string;
  fname:string;
  lname:string;
  age:number;
  country:string;
  state:string;
  city:string;
}